<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Boxes.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Boxes.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image 
                Grid.Column="0"
                Source="ms-appx:///Assets/WindowIcon.png"
                Width="16" 
                Height="16"
                Margin="16,0,12,0"
                VerticalAlignment="Center"/>

            <TextBlock 
                Grid.Column="0"
                Text="📦 Boxes"
                FontSize="13"
                FontWeight="SemiBold"
                VerticalAlignment="Center"
                Margin="16,0,0,0"/>

            <CommandBar 
                Grid.Column="2"
                Background="Transparent"
                DefaultLabelPosition="Right"
                IsOpen="False">
                <AppBarButton 
                    Icon="Refresh" 
                    Label="Refresh"
                    Command="{x:Bind ViewModel.RefreshDesktopCommand}"/>
                <AppBarButton 
                    Icon="Setting" 
                    Label="Settings"/>
            </CommandBar>
        </Grid>

        <!-- Main Content Area -->
        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            IsPaneToggleButtonVisible="True"
            OpenPaneLength="256"
            PaneDisplayMode="Left"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem 
                    Content="Overview" 
                    Icon="Home"
                    Tag="Overview"
                    IsSelected="True"/>
                <NavigationViewItem 
                    Content="Setup Library" 
                    Tag="SetupLibrary">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem 
                    Content="Desktop Files" 
                    Icon="Folder"
                    Tag="DesktopFiles"/>
                <NavigationViewItem 
                    Content="Categories" 
                    Icon="Library"
                    Tag="Categories"/>
                <NavigationViewItemSeparator/>
                <NavigationViewItem 
                    Content="Rules" 
                    Icon="Filter"
                    Tag="Rules"/>
                <NavigationViewItem 
                    Content="History" 
                    Icon="Clock"
                    Tag="History"/>
            </NavigationView.MenuItems>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame"/>
        </NavigationView>

    </Grid>
</Window>
